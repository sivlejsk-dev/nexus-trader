# Server-side backend URL used by Next.js rewrites.
# Browser code should call /api/* and let Next proxy to FastAPI.
BACKEND_API_URL=http://localhost:8000

# Legacy fallback for rewrites only. Do not use this for browser fetches.
NEXT_PUBLIC_API_URL=
